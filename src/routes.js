const routes = {
    root: "/",
    cart: "/cart",
    checkout: "/checkout",
    products: {
      productList: "/products",
      productListItem: "/products/:slug",
    },
  };
  export default routes;